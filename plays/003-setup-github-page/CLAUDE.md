# Play `003` — session instructions

Your session starts at the root of the project's **specs repository** — the one
holding `documentation/` and `plays/`. This play is `plays/003-setup-github-page/`.

Your primary instructions for this work are in the intent document. Read it
first and treat it as the source of truth for what to build:

- [Intent](intent.md)

Read `documentation/context/` before starting: those documents are shared by every
play and apply to all work in this project.

## Repositories

The project's repositories are on this machine at:

- `metro` — `C:\Users\Hugo\.building\data\projects\001-metro\repos\metro`
